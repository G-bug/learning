spring:
  application:
    name: consul-producer
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: consul-producer
---
spring:
  profiles: pro1

server:
  port: 8501

---
spring:
  profiles: pro2

server:
  port: 8503